import { Form } from 'react-bootstrap';

const Filter = () => {
  return (
    <div className="p-5 filter">
      <Form.Select size="lg">
        <option>Large select</option>
      </Form.Select>
    </div>
  );
};

export default Filter;
